let global: struct {
	title: str,
	version: str,
	ssh_pubkey: str,
	ssh_fp: str,
} = struct {
	title: str = "Test Forge",
	version: str = "v0.0.0",
	ssh_pubkey: str = "pubkey",
	ssh_fp: str = "fp",
};

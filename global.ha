let global: struct {
	title: str,
	version: str,
	ssh_pubkey: str,
	ssh_fp: str,
} = struct {
	title: str = "Test Forge",
	version: str = VERSION,
	ssh_pubkey: str = "pubkey",
	ssh_fp: str = "fp",
};

# Lindenii Forge

## State of the project

* The most active maintainer, [Runxi Yu](https://runxiyu.org), is a bit burnt
  out and needs some rest before continuing development.
* We haven't even completely decided on a programming language to implement
  things in, yet. Check out the `hare` and `go` branches though.
* The public-facing forges run the version from the `go` branch.
